---
layout: layouts/page.njk
title: Portfolio
description:

accentColor: 
headerImage:
headerColor:
backgroundcolor: dbdbdb

permalink: /portfolio/
eleventyNavigation:
  key: Portfolio
  order: 2
---


<div class="container">

<h2 class="">Update in progress I swear.</h2>
<p>Cosby sweater plaid shabby chic kitsch pour-over ex. Try-hard fanny pack mumblecore cornhole cray scenester. Assumenda narwhal occupy, Blue Bottle nihil culpa fingerstache. Meggings kogi vinyl meh, food truck banh mi Etsy magna 90's duis typewriter banjo organic leggings Vice.</p><h3 class="">Update in progress I swear.</h3>

{% set postslist = collections.folio %}

{% for post in postslist | reverse %}
  <div class="paneled">
    <a style="" href="{{ post.url | url }}" class="postlist-link postpanel">{% if post.data.title %}{{ post.data.title }}{% else %}<code>{{ post.url }}</code>{% endif %}
    </a>
    
    <br class="mobilebreak">
    
    <time class="postlist-date" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
    {% for tag in post.data.tags %}
      {%- if collections.tagList.indexOf(tag) != -1 -%}
      {% set tagUrl %}/tags/{{ tag }}/{% endset %}
      <a href="{{ tagUrl | url }}" class="tag">{{ tag }}</a>
      {%- endif -%}
    {% endfor %}
  </div>
{% endfor %}

</div>
